version: 4.1.35
steps:
  - prompt: Load the webpage.
    commands:
      - command: press-keys
        keys:
          - enter
  - prompt: >-
      Assert that the logo is visible on the page.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: the text "Example Domain" is visible
